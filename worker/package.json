{
  "name": "onse-studio-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply studio-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply studio-db --remote"
  },
  "dependencies": {
    "itty-router": "^5.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "~5.5.4",
    "wrangler": "^3.114.7"
  }
}
